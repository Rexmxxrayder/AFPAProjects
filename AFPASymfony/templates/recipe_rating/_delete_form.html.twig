<form method="post" action="{{ path('app_recipe_rating_delete', {'id': recipe_rating.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recipe_rating.id) }}">
    <button class="btn">Delete</button>
</form>
