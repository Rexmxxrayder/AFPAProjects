{% extends 'base.html.twig' %}

{% block title %}RecipeRating{% endblock %}

{% block body %}
    <h1>RecipeRating</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recipe_rating.id }}</td>
            </tr>
            <tr>
                <th>Rate</th>
                <td>{{ recipe_rating.rate }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recipe_rating_index') }}">back to list</a>

    <a href="{{ path('app_recipe_rating_edit', {'id': recipe_rating.id}) }}">edit</a>

    {{ include('recipe_rating/_delete_form.html.twig') }}
{% endblock %}
